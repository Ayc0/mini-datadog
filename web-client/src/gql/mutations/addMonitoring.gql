mutation addMonitoring(
  $url: String!
  $minutes: Int
  $seconds: Int
  $millis: Int
) {
  addMonitoring(
    url: $url
    minutes: $minutes
    seconds: $seconds
    millis: $millis
  ) {
    id
    url
    checkInterval
  }
}
